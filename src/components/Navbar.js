import styled from 'styled-components';

const Navbar = styled.div`
    position: fixed;
    width: 100vw;
    height: 70px;
    color: white;
    display: flex;
    align-items: center;
    top: 0px;
    border-bottom: 1px solid #123421;
    background-color: rgba(255,255,255,0.7)
`;

export default Navbar;
